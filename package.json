{
  "name": "diet-ui",
  "version": "0.0.1",
  "main": "packages/core/build/index.js",
  "module": "packages/core/build/index.esm.js",
  "files": [
    "build"
  ],
  "types": "build/index.d.ts",
  "scripts": {
    "build": "lerna run --parallel --scope  \"diet-ui\" build",
    "test": "lerna run test --since",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
